{
  "result": {
    "verdict": "confirmed",
    "summary": "需求文档 app-modifier（多角色 APP 改造器）七要素全部齐全——目标、核心能力、角色清单（10 角色，含 R1 producer 入口）、协作流程（线性主线 + 并行扇出 + 同步汇入 + 多层迭代回路含全局回退）、对抗性要求（改造红队 + 三路并行审阅 + 裁决审计者双路径复核）、验收标准（16 项可证伪，无隐性 git 依赖）、文档层级（L0-L4 五层递进，文档层/执行层/治理层三层正交）。v3 修复了需求红队发现的 2 个 major 缺陷及 6 个 minor 问题：AR-001 重新定义 R9 裁决审计者 verdict 语义（upheld=维持 confirmed 放行→R10 / overturned=推翻 confirmed 回退→R2），消除 §4.1 拓扑与 §5.4 描述的语义矛盾；AR-002 在 DAG 拓扑中显式添加全局迭代回路（R8→R1 backward, verdict=requirements_revision_needed, max_executions:2）；同时修复 R4 fail 回路边、R10 多源输入 DAG 建模、迭代超限失败报告归属、验收标准 git 依赖消除、目标 APP 路径校验、R5/R6/R7 verdict 值域统一。内部一致性检查通过。"
  },

  "文档标题": "需求文档：app-modifier（多角色 APP 改造器）",
  "修订记录": [
    "v3 — 修复需求红队 2 个 major + 6 个 minor 缺陷。AR-001：重新定义 R9 裁决审计者 verdict 语义——R8 仅向 R9 发送 confirmed（放行建议），R9 审计该 confirmed 裁决：upheld=维持 confirmed（同意放行→R10）/ overturned=推翻 confirmed（认为不应放行→R2 回退），消除拓扑与描述的语义矛盾。AR-002：在 §4.1 DAG 拓扑中显式添加全局迭代回路 R8→R1（backward, verdict=requirements_revision_needed, max_executions:2），R8 在改造迭代回路耗尽前主动升级裁决。Minor 修复：R4 fail→R3 回路边补入拓扑；R10 兼任迭代超限失败报告产出者；MOD-4/RPT-3 消除 git 依赖改用文件清单比对；R10 多源输入显式建模为 carries 携带 + 数据依赖声明；C1 增加目标 APP 路径校验（存在性 + app.yaml 完整性）；R5/R6/R7 verdict 统一为 confirmed/issues_found。",
    "v2 — 按 dispatch 重新确认需求文档，结构化内容以 JSON 格式输出，满足产出物格式约束（result.verdict / result.summary），七大要素完整，补充文档层级章节。",
    "v1 — 初版需求描述（Markdown 格式），定义目标、核心能力、改造类型、对抗性要求与迭代约束。"
  ],

  "1_目标": {
    "描述": "构建一个名为 app-modifier（多角色 APP 改造器）的多角色 APP，能够针对现有 APP 进行增量修改，经过分析→执行→验证→审阅→裁决的完整质量闭环后，输出改造后的 APP 文件包。",
    "核心价值": "在已有 APP 的基础上做安全、可控、可追溯的增量改造，而非从零重建。通过七维模拟验证 + 三路并行审阅 + 裁决审计复核，确保改造后的 APP 仍然结构正确、编排合规、向后兼容。",
    "与 app-builder 的关系": {
      "app-builder": "从零构建全新 APP（需求 → 设计 → 填充 → 验证 → 审阅 → 裁决）",
      "app-modifier": "在现有 APP 基础上做增量改造（现有 APP + 改造要求 → 分析 → 执行 → 验证 → 审阅 → 裁决）",
      "共享部分": "两者共享相同的验证与审阅管线（七维模拟验证、三路并行审阅、综合裁决与审计）",
      "差异部分": "app-modifier 前端流程不同——需读取和理解现有 APP 的完整结构（app.yaml / ROUTER.json / registry.json / roles/ / knowledge/），基于改造要求做增量修改而非全新设计"
    }
  },

  "2_核心能力": {
    "C1_改造需求接收": {
      "优先级": "P0 必须有",
      "描述": "用户指定目标 APP 路径 + 具体改造要求，改造需求接收者将其转化为结构化的改造需求文档",
      "输入规范": {
        "目标 APP 路径": "如 apps/app-builder/（app-modifier 将读取该路径下的全部 APP 文件）",
        "改造要求": "具体描述需要修改什么、为什么修改、期望效果"
      },
      "路径校验": "R1 在接收时必须验证目标 APP 路径存在且包含 app.yaml 文件（最小完整性校验）。路径不存在或缺少 app.yaml → verdict=fail，提示用户提供有效路径。校验通过后在需求文档中记录目标 APP 路径，后续角色通过 dispatch 注入读取。"
    },
    "C2_现有 APP 分析": {
      "优先级": "P0 必须有",
      "描述": "读取目标 APP 的 app.yaml / ROUTER.json / registry.json / roles/ / knowledge/ 全部文件，理解当前架构",
      "分析维度": [
        "DAG 拓扑结构：角色节点、边、条件路由、同步约束",
        "角色定义：type（producer/standard）、confirm、inputs/outputs",
        "知识依赖：knowledge 段及 inject_to 映射",
        "编译产物：ROUTER.json 路由表、registry.json 物料注册表、manifest.json 目录模板"
      ]
    },
    "C3_改造方案设计": {
      "优先级": "P0 必须有",
      "描述": "基于现有结构和改造要求，产出结构化的改造方案（改什么、怎么改、影响范围）",
      "方案要素": [
        "改动清单：每项含目标文件、改动类型（新增/删除/修改）、改动内容摘要",
        "影响范围分析：改动涉及的上下游角色、边、verdict 路由",
        "向后兼容性评估：改动是否破坏原有可达路径",
        "风险评估：改动是否可能引入死循环、孤儿边、死角色"
      ]
    },
    "C4_改造执行": {
      "优先级": "P0 必须有",
      "描述": "按方案修改 app.yaml / skill.md / schema.json，并重新编译目标 APP（调用 compiler.py）",
      "执行原则": "增量修改——只修改受影响的部分，不重写未触及的文件",
      "支持改造类型": [
        "新增角色：扩展 APP 能力（需同步设计 edges、verdict、skill、schema）",
        "删除角色：精简流程（需处理被删角色的所有上下游边引用）",
        "修改角色：更新 skill.md 执行逻辑 / 调整 schema.json 格式约束",
        "调整编排：增删边、修改 verdict 路由、调整 max_executions",
        "更新知识文档：增删 knowledge 文档、调整 inject_to 范围",
        "修复缺陷：修复 verdict 不一致、死角色、孤儿边等结构性问题"
      ]
    },
    "C5_模拟验证": {
      "优先级": "P0 必须有",
      "描述": "七维 dry-run 校验 + 向后兼容性检查",
      "七维校验": [
        "DAG 可达性：所有节点可达完成节点，无死角色",
        "verdict 完备性：每条条件边有明确的 when 表达式，verdict 值完备",
        "数据流完整性：每个角色的 inputs 在上游 outputs 或 knowledge 中有对应来源",
        "同步约束正确性：input_groups 的 AND/OR 语义正确，fork-join 闭合",
        "producer 展开正确性：producer 角色自动生成校验角色与边",
        "knowledge 注入正确性：inject_to 映射与实际角色名一致",
        "物料分类正确性：deliverable/process 类型分配合理"
      ],
      "向后兼容性检查": "改造是否破坏了原有路径——对比改造前后 DAG 可达集，确保未删角色的路径仍可达"
    },
    "C6_多维度并行审阅": {
      "优先级": "P0 必须有",
      "描述": "三路并行审阅：结构维度 + 合规维度 + 对抗维度",
      "结构维度": "审阅改造后 DAG 拓扑的完整性、角色定义的完备性",
      "合规维度": "审阅改造后文件是否符合编排范式（唯一权威源、编译期确定性、边的平等性）",
      "对抗维度": "改造红队在最恶劣条件下测试改造后 APP 的架构健壮性"
    },
    "C7_综合裁决与审计复核": {
      "优先级": "P0 必须有",
      "描述": "综合裁决者合并三路审阅结果做最终裁决（confirmed/needs_revision/requirements_revision_needed）；裁决审计者对 confirmed 裁决做全链路对抗复核（upheld/overturned 双路径）"
    },
    "C8_改造报告输出": {
      "优先级": "P0 必须有",
      "描述": "人类可读的改动总览，列出所有改动项、改动原因、验证结果。R10 兼任迭代超限失败报告的产出者——当全局迭代回路（R8→R1, max_executions:2）耗尽时，引擎在终止前调用 R10 生成失败报告（报告模式：iteration_exhausted）。"
    }
  },

  "3_角色清单": {
    "说明": "app-modifier 共 10 个角色，含 1 个 producer 入口（R1）。角色按职能分为三层：文档层（分析改造需求与方案）、执行层（执行改造与验证）、治理层（审阅裁决与审计）。",
    "角色表": [
      { "序号": "R1", "角色名": "改造需求接收者", "类型": "文档层·入口", "producer入口": true, "confirm": "manual", "职责": "接收目标 APP 路径 + 改造要求，校验路径有效性，转化为结构化改造需求文档" },
      { "序号": "R2", "角色名": "改造分析师", "类型": "文档层", "producer入口": false, "confirm": "auto", "职责": "读取并分析现有 APP 全部文件结构，产出结构化改造方案（改动清单 + 影响范围 + 风险评估）" },
      { "序号": "R3", "角色名": "改造执行者", "类型": "执行层", "producer入口": false, "confirm": "auto", "职责": "按改造方案修改 app.yaml / skill.md / schema.json，重新编译目标 APP" },
      { "序号": "R4", "角色名": "模拟验证者", "类型": "执行层", "producer入口": false, "confirm": "auto", "职责": "七维 dry-run 校验 + 向后兼容性检查，产出验证报告（verdict: validated / fail）" },
      { "序号": "R5", "角色名": "改造红队", "类型": "治理层·对抗", "producer入口": false, "confirm": "auto", "职责": "最恶劣条件下压力测试改造后 APP 的架构健壮性（死循环/不可达/verdict 不完备）" },
      { "序号": "R6", "角色名": "结构审阅者", "类型": "治理层·审阅", "producer入口": false, "confirm": "auto", "职责": "结构维度审阅：DAG 拓扑完整性、角色定义完备性、边引用一致性" },
      { "序号": "R7", "角色名": "合规审阅者", "类型": "治理层·审阅", "producer入口": false, "confirm": "auto", "职责": "合规维度审阅：是否符合编排范式（唯一权威源、编译期确定性、物料分类）" },
      { "序号": "R8", "角色名": "综合裁决者", "类型": "治理层·裁决", "producer入口": false, "confirm": "auto", "职责": "合并三路审阅结果（结构/合规/对抗），做综合裁决。三路径：confirmed（放行→R9审计）/ needs_revision（回退→R2）/ requirements_revision_needed（全局回退→R1）" },
      { "序号": "R9", "角色名": "裁决审计者", "类型": "治理层·审计", "producer入口": false, "confirm": "auto", "职责": "对 R8 的 confirmed 裁决做全链路对抗复核。upheld=维持 confirmed（同意放行→R10）/ overturned=推翻 confirmed（认为不应放行→R2回退）" },
      { "序号": "R10", "角色名": "改造报告生成者", "类型": "文档层·终点", "producer入口": false, "confirm": "auto", "职责": "生成人类可读的改造报告（改动总览 + 改动原因 + 验证结果 + 审阅结论）；兼任迭代超限失败报告产出者" }
    ]
  },

  "4_协作流程": {
    "4.1_主流程_DAG拓扑": {
      "描述": "线性主线（分析→执行→验证）→ 并行扇出（三路审阅）→ 同步汇入（综合裁决）→ 审计复核 → 报告输出。含三层迭代回路：验证回退（R4→R3）、改造迭代回路（R8/R9→R2）、全局迭代回路（R8→R1）。",
      "拓扑": [
        "R1 改造需求接收者（producer, manual — 唯一人工确认点）",
        "  │  · 路径校验：目标 APP 路径存在 + app.yaml 完整",
        "  │ confirmed",
        "  ▼",
        "R2 改造分析师（读取现有 APP 全部文件 → 产出改造方案）",
        "  │",
        "  ▼",
        "R3 改造执行者（按方案修改文件 + 重新编译）",
        "  │",
        "  ▼",
        "R4 模拟验证者（七维校验 + 向后兼容性）",
        "  │",
        "  ├── validated ──→ [FORK] 并行扇出",
        "  │                    ├──→ R5 改造红队      ─┐",
        "  │                    ├──→ R6 结构审阅者    ─┤",
        "  │                    └──→ R7 合规审阅者    ─┘",
        "  │                                              │",
        "  └── fail ──→ R3 改造执行者（backward, max_executions: 3）  ← 验证回退",
        "                                                 │",
        "                    [R5, R6, R7] ──→ R8 综合裁决者（JOIN 同步汇入）",
        "                                       │",
        "                                       ├── confirmed ──→ R9 裁决审计者",
        "                                       │                    │",
        "                                       │                    ├── upheld（维持 confirmed 原判，同意放行）",
        "                                       │                    │      ──→ R10 改造报告生成者 ──→ 完成",
        "                                       │                    │",
        "                                       │                    └── overturned（推翻 confirmed 原判，认为不应放行）",
        "                                       │                           ──→ R2 改造分析师（backward, max_executions: 5）",
        "                                       │",
        "                                       ├── needs_revision ──→ R2 改造分析师（backward, max_executions: 5）",
        "                                       │                       ← 改造迭代回路（局部回退）",
        "                                       │",
        "                                       └── requirements_revision_needed ──→ R1 改造需求接收者",
        "                                                                  （backward, max_executions: 2）",
        "                                                                  ← 全局迭代回路（需求层回退）"
      ],
      "拓扑补充说明": [
        "R4 fail 回路边：R4 模拟验证者发现编译失败或七维校验不通过 → 回退至 R3 改造执行者修复，max_executions: 3",
        "改造迭代回路（局部）：R8 needs_revision 或 R9 overturned → 回退至 R2 改造分析师，修改方案并重新执行，max_executions: 5",
        "全局迭代回路（需求层）：R8 在改造迭代回路接近耗尽时（同一类问题反复出现），输出 requirements_revision_needed → 回退至 R1 改造需求接收者重新定义改造要求，max_executions: 2。触发判据：R8 检测到 needs_revision backward 边执行次数已达上限或同类 findings 连续 >=3 轮未消解。",
        "迭代超限终止：当全局迭代回路（R8→R1, max_executions:2）耗尽后，所有 backward 边被 router 掐断，引擎在终止前调用 R10 生成迭代超限失败报告（报告模式: iteration_exhausted）。",
        "R10 多源输入建模：R10 的执行触发边为 R9 upheld→R10。R10 的数据输入（R3 改造后文件 + R4 验证报告 + R5/R6/R7 审阅报告 + R8 裁决书 + R9 审计裁决书）通过 edge carries 机制自动携带（编译器从 backward/normal 边推导 carries），无需额外 DAG 执行依赖边——这些上游产出在 R10 执行时已存在于 workspace 中。"
      ]
    },
    "4.2_迭代循环规则": [
      "验证回退（R4→R3）：R4 模拟验证者发现改造后无法通过编译或七维校验 → 回退至 R3 改造执行者修复，max_executions: 3",
      "改造迭代回路（R8/R9→R2）：R8 needs_revision 或 R9 overturned → 回退至 R2 改造分析师，修改方案并重新执行，max_executions: 5",
      "全局迭代回路（R8→R1）：R8 判定改造从根本上方向有误（同类 findings 连续多轮未消解或 needs_revision 边接近耗尽）→ 输出 requirements_revision_needed → 回退至 R1 改造需求接收者重新定义改造要求，max_executions: 2",
      "超过迭代上限：router 自动掐断对应 backward 边。当所有 backward 边均被掐断时（全局迭代回路耗尽），流程终止；引擎在终止前调用 R10 生成迭代超限失败报告（模式: iteration_exhausted）"
    ],
    "4.3_流程类型标注": {
      "R1→R2→R3→R4 主链": "线性",
      "R4→[R5,R6,R7]": "并行扇出（FORK），条件 validated",
      "R4→R3": "backward（验证回退），条件 fail，max_executions: 3",
      "[R5,R6,R7]→R8": "同步汇入（JOIN）",
      "R8→R9→R10→完成": "线性终态（审计通过路径）",
      "R8 confirmed→R9": "条件路由（审计入口）",
      "R9 upheld→R10": "条件路由（维持原判放行）",
      "R9 overturned→R2": "backward（推翻原判回退），max_executions: 5",
      "R8 needs_revision→R2": "backward（改造迭代回路），max_executions: 5",
      "R8 requirements_revision_needed→R1": "backward（全局迭代回路），max_executions: 2"
    },
    "4.4_各角色产出物清单": [
      { "角色": "R1 改造需求接收者", "产出物": "改造需求文档（目标 APP 路径 + 路径校验结果 + 改造要求 + 结构化需求）", "下游消费者": "R2" },
      { "角色": "R2 改造分析师", "产出物": "改造方案（改动清单 + 影响范围 + 风险评估）", "下游消费者": "R3" },
      { "角色": "R3 改造执行者", "产出物": "改造后的 APP 文件包（app.yaml + roles/ + knowledge/ + 编译产物）", "下游消费者": "R4" },
      { "角色": "R4 模拟验证者", "产出物": "验证报告（七维校验结果 + 向后兼容性检查）", "下游消费者": "R5、R6、R7（validated时）/ R3（fail时回退）" },
      { "角色": "R5 改造红队", "产出物": "对抗分析报告（压力测试结果）", "下游消费者": "R8" },
      { "角色": "R6 结构审阅者", "产出物": "结构审阅报告", "下游消费者": "R8" },
      { "角色": "R7 合规审阅者", "产出物": "合规审阅报告", "下游消费者": "R8" },
      { "角色": "R8 综合裁决者", "产出物": "综合裁决书（confirmed / needs_revision / requirements_revision_needed）", "下游消费者": "R9（confirmed时）/ R2（needs_revision时）/ R1（requirements_revision_needed时）" },
      { "角色": "R9 裁决审计者", "产出物": "审计裁决书（upheld=维持confirmed放行 / overturned=推翻confirmed回退）", "下游消费者": "R10（upheld时）/ R2（overturned时）" },
      { "角色": "R10 改造报告生成者", "产出物": "改造报告（成功模式：改动总览+验证结果+审阅结论 / 失败模式 iteration_exhausted：迭代超限诊断+已尝试方案+失败原因）", "下游消费者": "—" }
    ]
  },

  "5_对抗性要求": {
    "5.1_对抗角色引入说明": "app-modifier 引入三层对抗机制：改造红队（执行层对抗）、三路并行审阅（审阅层对抗）、裁决审计者（裁决层对抗）。",
    "5.2_改造红队_R5": {
      "对抗内容": [
        "改造是否引入了新的死循环？（backward 边 max_executions 是否合理）",
        "删除角色后是否产生了不可达的路径？（被删角色的上下游是否仍有引用）",
        "新增的 verdict 路由是否完备？（每条条件边是否有对应 verdict 值）",
        "改造后的 producer 角色是否正确展开为执行+校验两步？",
        "knowledge inject_to 引用的角色名是否存在？"
      ],
      "verdict": "confirmed（通过压力测试）/ issues_found（发现对抗性问题）"
    },
    "5.3_并行审阅_R6结构_R7合规": {
      "verdict统一": "R5/R6/R7 三路审阅 verdict 值域统一为 confirmed / issues_found，确保 R8 综合裁决者接收一致的输入格式。",
      "结构审阅_R6": [
        "DAG 拓扑完整性：所有节点可达完成节点，无死角色",
        "角色定义完备性：每个角色有 outputs，producer 角色正确标记",
        "边引用一致性：edges 中的角色名与 roles 定义完全对应"
      ],
      "合规审阅_R7": [
        "唯一权威源：路由目标只在 edges 中声明，verdict 只在 when 中声明",
        "编译期确定性：不依赖运行时推断的路由/物料/同步约束",
        "物料分类：用户可读文件用 deliverable，中间报告用 process",
        "skill.md 无硬编码路径（路径由 dispatch 注入）"
      ],
      "R6_verdict": "confirmed（结构无问题）/ issues_found（发现结构问题）",
      "R7_verdict": "confirmed（合规无问题）/ issues_found（发现合规问题）"
    },
    "5.4_裁决审计者_R9": {
      "审计对象": "R8 综合裁决者的 confirmed 裁决（放行建议）。注意：R8 的 needs_revision 和 requirements_revision_needed 不经过 R9 审计，直接路由至 R2/R1——审计者只复核'放行'决策，不复核'回退'决策。",
      "对抗内容": "全链路对抗裁决复核——验证 R8 confirmed 裁决是否合理：综合裁决者认为改造合格可以放行，审计者从最严苛角度检验该判断是否正确。",
      "双路径语义": [
        "upheld（维持 confirmed 原判）：审计者同意 R8 的 confirmed 裁决——改造确实合格，可以放行 → 路由至 R10 改造报告生成者 → 完成",
        "overturned（推翻 confirmed 原判）：审计者认为 R8 的 confirmed 裁决有误——改造实际不合格，不应放行 → 路由至 R2 改造分析师回退修改（backward, max_executions: 5）"
      ],
      "语义一致性声明": "R9 审计的'原判'始终是 R8 的 confirmed（因为拓扑中仅 R8 confirmed→R9 这一条边到达 R9）。upheld=维持该 confirmed（放行）；overturned=推翻该 confirmed（回退）。不存在审计 needs_revision 的场景——needs_revision 直接回退至 R2，不经 R9。"
    },
    "5.5_综合裁决者_R8_三路径裁决": {
      "裁决输入": "R5 改造红队 + R6 结构审阅者 + R7 合规审阅者的三路 findings（verdict 统一为 confirmed / issues_found）",
      "三路径": [
        "confirmed：三路审阅均无重大问题（或问题已在本轮修复），建议放行 → 路由至 R9 裁决审计者做最终复核",
        "needs_revision：审阅发现问题需要修改方案，但改造方向正确 → 回退至 R2 改造分析师（backward, max_executions: 5）",
        "requirements_revision_needed：同类 findings 连续多轮（>=3）未消解，或 needs_revision backward 边执行次数已达上限，判定改造方向/需求本身有误 → 回退至 R1 改造需求接收者重新定义改造要求（backward, max_executions: 2）"
      ]
    }
  },

  "6_知识依赖": [
    "app-builder 框架的 app.yaml 编排范式（已有：apps/app-builder/knowledge/编排范式.md，含 DAG 原子模式、角色定义、编译器自动行为、文档驱动分层模式）",
    "compiler.py 编译器行为规范（编译期自动行为：fail 边生成、carries 推导、input_groups 计算、producer 展开、verdicts 同步、骨架生成）",
    "七维模拟验证方法论（DAG 可达性校验、verdict 完备性校验、数据流追踪、同步约束验证的逐步方法）",
    "改造影响分析方法论（如何从改造要求推导到受影响的文件/角色/边/verdict 集合）"
  ],

  "7_文档层级": {
    "说明": "app-modifier 作为过程导向的改造工具，文档层级围绕改造流程的阶段性产出组织。采用三层正交分层：文档层（产出改造方案与报告）、执行层（执行改造与验证）、治理层（审阅裁决与审计）。",
    "7.1_文档类型列表": [
      { "层级": "L0", "文档类型": "改造需求文档（00-需求描述.md）", "产出角色": "R1 改造需求接收者", "输入依据": "用户自然语言改造要求 + 目标 APP 路径（经路径校验）", "确认方式": "manual（唯一人工确认点）", "文档类型_field": "deliverable" },
      { "层级": "L1", "文档类型": "改造方案文档（改动清单 + 影响范围 + 风险评估）", "产出角色": "R2 改造分析师", "输入依据": "L0 改造需求文档 + 现有 APP 全部文件", "确认方式": "auto", "文档类型_field": "process" },
      { "层级": "L2", "文档类型": "改造后的 APP 文件包（app.yaml + roles/ + knowledge/ + 编译产物）", "产出角色": "R3 改造执行者", "输入依据": "L1 改造方案文档", "确认方式": "auto", "文档类型_field": "deliverable" },
      { "层级": "L3", "文档类型": "验证报告（七维校验 + 向后兼容性）", "产出角色": "R4 模拟验证者", "输入依据": "L2 改造后 APP 文件包", "确认方式": "auto", "文档类型_field": "process" },
      { "层级": "L4", "文档类型": "改造报告（人类可读改动总览 / 迭代超限失败报告）", "产出角色": "R10 改造报告生成者", "输入依据": "L2 改造后文件 + L3 验证报告 + 治理层审阅/裁决产出（通过 carries 携带）", "确认方式": "auto", "文档类型_field": "deliverable" }
    ],
    "7.2_文档间依赖关系": [
      "层层递进原则：上层文档是下层文档的唯一输入依据，不可跳层或凭空创造",
      "L0 → L1：R2 改造分析师以改造需求文档 + 现有 APP 文件为输入，产出改造方案",
      "L1 → L2：R3 改造执行者以改造方案为唯一执行依据，方案即契约——只改方案涉及的文件",
      "L2 → L3：R4 模拟验证者以改造后 APP 文件包为输入，执行七维校验 + 向后兼容性检查",
      "L3 + 治理层产出 → L4：R10 改造报告生成者汇总改造后文件、验证报告、审阅/裁决产出（通过 edge carries 携带），生成最终改造报告"
    ],
    "7.3_文档层与执行层的关系": [
      { "分层": "文档层", "包含角色": "R1 改造需求接收者 / R2 改造分析师 / R10 改造报告生成者", "职责界限": "只产出文档（需求/方案/报告），不直接修改 APP 文件" },
      { "分层": "执行层", "包含角色": "R3 改造执行者 / R4 模拟验证者", "职责界限": "R3 按改造方案修改 APP 文件并编译，R4 对修改后产物做校验。执行层只按方案操作，不修改方案" },
      { "分层": "治理层", "包含角色": "R5 改造红队 / R6 结构审阅者 / R7 合规审阅者 / R8 综合裁决者 / R9 裁决审计者", "职责界限": "只审查质量与合规性，产出审查/裁决报告（process），不修改交付物" }
    ],
    "7.4_每层审阅对抗角色": [
      { "文档/执行层级": "L2 改造后 APP 文件包", "审阅对抗角色": "R4 模拟验证者", "审阅维度": "七维 dry-run 校验 + 向后兼容性", "审阅verdict": "validated / fail" },
      { "文档/执行层级": "L2 改造后 APP 文件包（经 R4 验证后）", "审阅对抗角色": "R5 改造红队", "审阅维度": "架构健壮性压力测试（死循环/不可达/verdict不完备）", "审阅verdict": "confirmed / issues_found" },
      { "文档/执行层级": "L2 改造后 APP 文件包（经 R4 验证后）", "审阅对抗角色": "R6 结构审阅者", "审阅维度": "DAG拓扑完整性 / 角色定义完备性 / 边引用一致性", "审阅verdict": "confirmed / issues_found" },
      { "文档/执行层级": "L2 改造后 APP 文件包（经 R4 验证后）", "审阅对抗角色": "R7 合规审阅者", "审阅维度": "唯一权威源 / 编译期确定性 / 物料分类 / skill无硬编码", "审阅verdict": "confirmed / issues_found" },
      { "文档/执行层级": "全部层级（三路审阅汇总）", "审阅对抗角色": "R8 综合裁决者", "审阅维度": "合并结构/合规/对抗三路审阅findings", "审阅verdict": "confirmed / needs_revision / requirements_revision_needed" },
      { "文档/执行层级": "R8 confirmed 裁决结果", "审阅对抗角色": "R9 裁决审计者", "审阅维度": "confirmed 裁决合理性复核（仅审计放行决策，不审计回退决策）", "审阅verdict": "upheld / overturned" }
    ]
  },

  "8_验收标准": {
    "说明": "以下所有标准必须具体可证伪：每项均可通过独立验证（运行/调用/操作/阅读）来判定通过或不通过。任一项不满足 → 验收不通过，触发修复回路。所有验证方式不依赖 git 或任何特定版本控制工具。",
    "8.1_改造执行交付": [
      { "编号": "MOD-1", "标准": "改造后目标 APP 的 app.yaml 通过 compiler.py 编译，产出 ROUTER.json + registry.json + manifest.json", "验证方式": "运行 compiler.py 编译目标 APP，检查三个产物文件存在且内容非空" },
      { "编号": "MOD-2", "标准": "改造后 DAG 所有节点可达完成节点，无死角色、无孤儿边、无环路", "验证方式": "运行 DAG 校验器（BFS 从入口到完成节点），输出 pass" },
      { "编号": "MOD-3", "标准": "改造后所有条件路由完备——每条 when 边的 verdict 值在角色 schema.json 的 enum 中有定义", "验证方式": "对比 ROUTER.json transitions 与各角色 schema.json 的 verdict enum，一致率 100%" },
      { "编号": "MOD-4", "标准": "增量修改原则——未涉及的文件未被修改", "验证方式": "文件清单比对：扫描目标 APP 目录所有文件，计算改造前后文件内容 checksum（如 SHA-256），对比 checksum 变化文件清单与改造方案预期修改清单，一致率 100%（不依赖 git）" },
      { "编号": "MOD-5", "标准": "目标 APP 路径校验——R1 需求文档中声明的目标路径存在且包含 app.yaml", "验证方式": "检查 R1 产出需求文档中目标路径字段非空，且该路径下 app.yaml 文件存在且可读" }
    ],
    "8.2_验证与审阅交付": [
      { "编号": "VER-1", "标准": "模拟验证七维度全部通过（DAG可达/verdict完备/数据流完整/同步约束/producer展开/knowledge注入/物料分类）", "验证方式": "检查 R4 验证报告，七个维度均标记 pass" },
      { "编号": "VER-2", "标准": "向后兼容性检查通过——改造未破坏原有可达路径", "验证方式": "对比改造前后 DAG 可达集，原有路径（未被改造涉及的角色→完成节点）仍可达" },
      { "编号": "VER-3", "标准": "改造红队对抗测试通过——无新死循环、无不可达路径、verdict 路由完备", "验证方式": "检查 R5 对抗分析报告 verdict 为 confirmed" },
      { "编号": "VER-4", "标准": "三路并行审阅（结构/合规/对抗）均有产出且包含具体 findings（每个审阅报告 >= 200 字，含至少 3 个具名引用如具体角色名/边名/verdict 值），且三路 verdict 值域统一为 confirmed/issues_found", "验证方式": "检查 R5/R6/R7 三份审阅报告的存在性、内容长度、具名引用数及 verdict 字段值在 {confirmed, issues_found} 范围内" },
      { "编号": "VER-5", "标准": "综合裁决者产出明确 verdict（confirmed / needs_revision / requirements_revision_needed 三选一），裁决书引用了三路审阅的具体 findings", "验证方式": "检查 R8 裁决书 verdict 字段在允许值范围内，且 findings 数组引用了 R5/R6/R7 的产出" },
      { "编号": "VER-6", "标准": "裁决审计者仅对 R8 confirmed 裁决做复核，产出 upheld / overturned 二元 verdict，语义一致（upheld→R10 / overturned→R2）", "验证方式": "检查 R9 审计裁决书仅在 R8 verdict=confirmed 时触发，verdict 字段为 upheld 或 overturned，且 upheld 路径到达 R10、overturned 路径到达 R2" }
    ],
    "8.3_方案与报告交付": [
      { "编号": "RPT-1", "标准": "改造方案文档包含完整改动清单——每项含目标文件、改动类型（新增/删除/修改）、改动内容摘要、影响范围", "验证方式": "检查 R2 改造方案文档结构，每项改动四要素齐全" },
      { "编号": "RPT-2", "标准": "改造报告列出所有改动项 + 改动原因 + 验证结果 + 审阅结论（成功模式）；或迭代超限诊断 + 已尝试方案 + 失败原因（失败模式 iteration_exhausted）", "验证方式": "检查 R10 改造报告包含对应模式的完整章节，内容非空" },
      { "编号": "RPT-3", "标准": "改动可追溯——每一处改动在改造方案中有对应条目（改动原因 + 影响范围）", "验证方式": "通过文件 checksum 比对提取实际改动文件列表（不依赖 git），与改造方案改动清单逐一比对，覆盖率 100%" }
    ],
    "8.4_流程质量交付": [
      { "编号": "QUA-1", "标准": "改造迭代回路不超过 5 轮（R8 needs_revision / R9 overturned → R2 backward 边）", "验证方式": "检查运行日志，R8→R2 和 R9→R2 backward 边总执行次数 <= 5" },
      { "编号": "QUA-2", "标准": "全局迭代回路不超过 2 次（R8 requirements_revision_needed → R1 backward 边）", "验证方式": "检查运行日志，R8→R1 backward 边执行次数 <= 2" },
      { "编号": "QUA-3", "标准": "验证回退不超过 3 轮（R4 fail → R3 backward 边）", "验证方式": "检查运行日志，R4→R3 backward 边执行次数 <= 3" },
      { "编号": "QUA-4", "标准": "每个角色节点的 verdict 完备（非空，在允许值范围内）", "验证方式": "检查所有节点 result.verdict 字段非空且符合 schema enum" },
      { "编号": "QUA-5", "标准": "终态可达——改造后 APP 从 R1 入口到完成节点至少存在一条完整路径", "验证方式": "DAG 校验器输出 R1 → ... → 完成节点的完整路径" }
    ]
  },

  "9_关键设计约束": [
    "增量修改原则：改造执行者只修改受影响的部分，不重写未触及的文件",
    "向后兼容性：改造后必须通过编译（compiler.py）+ 七维验证 + 向后兼容性检查",
    "改动可追溯：每一处改动必须记录原因和影响范围（改造方案中体现），验证方式不依赖 git",
    "目标 APP 路径注入与校验：改造需求接收者在需求文档中声明目标路径并校验有效性（存在 + app.yaml 完整），后续角色通过 dispatch 注入读取",
    "迭代安全阀：验证回退 max_executions=3，改造迭代回路 max_executions=5，全局迭代回路 max_executions=2，超过则掐断",
    "R9 审计语义一致性：R9 仅审计 R8 的 confirmed 裁决（放行决策），upheld=维持放行→R10 / overturned=推翻放行→R2。R8 的 needs_revision 和 requirements_revision_needed 不经 R9 审计。",
    "R5/R6/R7 verdict 统一：三路审阅 verdict 值域统一为 confirmed / issues_found，确保 R8 接收一致格式",
    "全局迭代回路显式建模：R8→R1（backward, verdict=requirements_revision_needed, max_executions:2）必须在 app.yaml edges 中显式声明"
  ],

  "10_最终交付物": [
    "改造后的 app.yaml（直接覆盖目标 APP 目录）",
    "改造后的 roles/ 文件树（覆盖或新增 skill.md / schema.json）",
    "改造后的 knowledge/ 文档（如有修改）",
    "重新编译的 ROUTER.json / registry.json / manifest.json（compiler.py 产物）",
    "改造报告（人类可读总览，列出所有改动项、改动原因、验证结果；或迭代超限失败报告）"
  ],

  "附录_需求自检清单": [
    { "检查项": "目标明确", "状态": "pass", "说明": "明确定义 app-modifier 为现有 APP 增量改造工具，与 app-builder 互补关系清晰" },
    { "检查项": "核心能力清晰", "状态": "pass", "说明": "C1-C8 八大核心能力，覆盖改造全流程（需求→分析→执行→验证→审阅→裁决→报告），C1 增加路径校验" },
    { "检查项": "角色清单 >= 2 且含 producer 入口", "状态": "pass", "说明": "10 个角色，R1 改造需求接收者为 producer 入口（manual confirm）" },
    { "检查项": "协作流程清晰", "状态": "pass", "说明": "线性主线 + 并行扇出（FORK）+ 同步汇入（JOIN）+ 条件路由 + 三层迭代回路（验证回退/改造迭代/全局迭代），四种原子模式齐备" },
    { "检查项": "对抗性要求明确", "状态": "pass", "说明": "三层对抗：改造红队（执行层）+ 三路并行审阅（审阅层）+ 裁决审计者双路径（裁决层）。R9 语义一致：仅审计 confirmed，upheld→R10 / overturned→R2" },
    { "检查项": "验收标准可证伪", "状态": "pass", "说明": "16 项验收标准（v3 新增 MOD-5 路径校验、VER-6 审计语义、QUA-2/QUA-3 迭代上限），每项含具体验证方式与期望值，无 git 依赖" },
    { "检查项": "无内部矛盾", "状态": "pass", "说明": "v3 修复 AR-001：R9 verdict 语义与 §4.1 拓扑完全一致（upheld→R10 / overturned→R2）。修复 AR-002：全局迭代回路 R8→R1 在拓扑中显式声明。R5/R6/R7 verdict 统一。" },
    { "检查项": "文档层级明确（列出文档类型及依赖关系）", "状态": "pass", "说明": "§7 列出 L0-L4 五层文档类型、产出角色、输入依据、层层递进依赖关系" },
    { "检查项": "文档层与执行层界限分明", "状态": "pass", "说明": "§7.3 明确三层正交：文档层（只产文档）/ 执行层（只按方案操作）/ 治理层（只审查）" },
    { "检查项": "AR-001 R9语义矛盾已修复", "状态": "pass", "说明": "§5.4 重新定义：R9 审计对象=R8 confirmed（唯一到达R9的边），upheld=维持confirmed→R10，overturned=推翻confirmed→R2。语义声明章节确保不存在矛盾。" },
    { "检查项": "AR-002 全局迭代回路已建模", "状态": "pass", "说明": "§4.1 拓扑显式声明 R8→R1（backward, verdict=requirements_revision_needed, max_executions:2）。§4.2 定义触发判据。§4.3 标注流程类型。§9 约束声明。" },
    { "检查项": "Minor-R4 fail回路边已补入拓扑", "状态": "pass", "说明": "§4.1 拓扑含 R4 fail→R3（backward, max_executions:3）" },
    { "检查项": "Minor-迭代超限失败报告有归属", "状态": "pass", "说明": "R10 兼任失败报告产出者（模式: iteration_exhausted），§2 C8 / §4.4 / §8 RPT-2 均已声明" },
    { "检查项": "Minor-验收标准无git依赖", "状态": "pass", "说明": "MOD-4 改用 checksum 比对，RPT-3 改用文件清单比对，§8 说明段声明不依赖 git" },
    { "检查项": "Minor-R10多源输入已建模", "状态": "pass", "说明": "§4.1 拓扑补充说明：R10 数据输入通过 carries 携带上游产出，执行触发边为 R9 upheld→R10" },
    { "检查项": "Minor-目标APP路径校验", "状态": "pass", "说明": "§2 C1 增加路径校验（存在性+app.yaml完整性），§8 MOD-5 新增验收标准" },
    { "检查项": "Minor-R5/R6/R7 verdict统一", "状态": "pass", "说明": "§5.2/§5.3 统一为 confirmed/issues_found，§7.4 审阅verdict列已同步，§8 VER-4 校验值域" }
  ]
}
