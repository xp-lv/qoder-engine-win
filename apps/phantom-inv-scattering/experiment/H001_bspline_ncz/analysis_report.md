# 分析报告 — exp_id: H001_bspline_ncz

## 三件套指标（result_status: VALID, 数据来源: 部分超时 iter 2 AGG）

| 指标 | 迭代值 | 基线值 | Δ | 预期 | 判定 |
|------|--------|--------|---|------|------|
| cos θ (1 GHz) | 0.328 | 0.9929 | -0.6649 | ≥0.9929 | ❌ 大幅退化 |
| F_cheb (multi-freq) | 0.3763 | 0.091 | +0.2853 | ≤0.080 | ❌ 大幅恶化 (4.1× 基线) |
| A12 PASS | ≤2/7 (预估) | 5/7 | ≤-3 | ≥6/7 | ❌ 远未达标 |

> 注: 反演在 iter 3 超时，仅有 iter 1-2 完整数据。最佳 F_cheb=0.3763 来自 iter 2（最后一次 accepted step 后的状态）。PASS 项数无法直接计算（需完成全部 10 轮迭代后由 A12_postprocess 判定），但 F_cheb=0.3763 远超 F_cheb ≤0.080 的通过线，预估 PASS ≤ 2/7。

## 反演行为分析

### 迭代收敛趋势
```
iter 1: F_cheb=0.4364 → Armijo ACCEPTED (mu=0.050, F_data 0.4364→0.3763)
iter 2: F_cheb=0.3763 → ALL 8 Armijo REJECTED (F_data 无法降低)
iter 3+: 超时（预计仍会停滞）
```

### 关键观察
1. **反演停滞**: iter 2 的 8 次 Armijo 线搜索全部被拒绝（F_data 从 0.4554 降至 0.3764 仍无法低于 old_data=0.3763），反演陷入局部最优
2. **重建质量差**: inner eps_r mean=3.719 (真值 5.0, 误差 25.6%), cos θ=0.328 表示重建场与观测场方向几乎不一致
3. **高频恶化**: 2 GHz 和 3 GHz 的 cos θ 为负值（-0.309, -0.035），表明高频成分重建完全失败
4. **n_cz=5 未带来改善**: 控制点 200→500 的增加反而使优化更困难（更多自由度 → 更大的参数搜索空间 → Armijo 更难满足）

## 改进方向建议

### 1. 问题诊断
n_cz=2→5 增加了 z 方向空间分辨率，但 500 控制点 vs 673 内层体素的约束/参数比从 1:0.30（200/673）变为 1:0.75（500/673），接近 1:1 导致欠约束。优化器在更大的参数空间中搜索困难，Armijo 准则难以满足。

### 2. 建议探索方向
- **方向 A**: 增大 lambda_tv（如 1e-4 → 1e-3），加强 TV 正则化以约束额外自由度
- **方向 B**: 降低 n_cz 至中间值（如 n_cz=3 或 n_cz=4），在分辨率和可优化性间取折中
- **方向 C**: 改进初始化策略（如从 n_cz=2 的结果热启动 n_cz=5 反演）
- **方向 D**: 放宽 Armijo 准则或改用 simple descent 步长策略

### 3. 结论
当前假设 H001 (n_cz=2→5) **不成立**。增加 z 方向控制点未改善重建精度，反而因参数空间增大导致优化困难和反演停滞。建议 M5 回填 FAIL 并启动新假设（方向 B: n_cz=3 折中方案）。
